@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()
        <ul class="nav navbar-nav">
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Maquinas Virtuales <span class="caret"></span></a>
                <ul class="dropdown-menu">
                    <li>@Html.ActionLink("Maquinas Virtuales", "Index_MVirtual", "MVirtual")</li>
                    <li>@Html.ActionLink("Grupos", "Index_MVirtual", "MVirtual")</li>
                </ul>
            </li>
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Politicas <span class="caret"></span></a>
                <ul class="dropdown-menu">
                    <li>@Html.ActionLink("Politicas", "Index_Politica", "Politica")</li>
                </ul>
            </li>
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Configuraciones <span class="caret"></span></a>
                <ul class="dropdown-menu">
                    <li>@Html.ActionLink("Comandos de Servidor", "Index_Politica", "Politica")</li>
                    <li>@Html.ActionLink("Usuarios SSH", "Index_Politica", "Politica")</li>
                </ul>
            </li>
        </ul>
    }
}